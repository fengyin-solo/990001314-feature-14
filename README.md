# 社区便民留言板

基于 PHP 原生开发的社区便民留言板网站，支持居民求助、意见建议、失物招领等功能。

## 功能特性

- **首页展示**：滚动显示最新留言信息，统计各类留言数量
- **留言发布**：用户可提交留言，选择类型（求助/建议/失物招领），支持图片上传
- **排序筛选**：支持按时间/热度排序，按类型筛选
- **后台管理**：管理员可审核、通过、拒绝、删除留言
- **会话管理**：管理员可查看当前账号的活跃会话，主动退出其他设备，退出后入口立即失效
- **登录风控**：连续登录失败自动锁定、会话空闲/超时过期，异常时明确提示并回到登录页
- **角色权限**：超级管理员 / 审核员 / 只读访客三种角色，越权访问与只读约束按权限执行
- **多标签页一致**：同一浏览器多个标签页共享会话状态，任一标签失效时全部提示并跳转
- **响应式布局**：适配手机和电脑端

## 技术栈

- **后端**：PHP 原生开发
- **数据库**：MySQL
- **前端**：HTML5 + CSS3 + JavaScript（原生）
- **特性**：响应式设计、图片上传、分页、搜索

## 安装部署

### 环境要求

- PHP >= 7.4
- MySQL >= 5.7
- Apache / Nginx

### 安装步骤

1. 将项目文件上传到 Web 服务器根目录

2. 访问安装脚本创建数据库：
   ```
   http://your-domain/install.php
   ```

3. 安装完成后删除 `install.php` 文件

4. 访问首页：
   ```
   http://your-domain/index.php
   ```

5. 访问后台：
   ```
   http://your-domain/admin/login.php
   ```

### 默认管理员账号

| 用户名 | 密码 | 角色 | 权限说明 |
| --- | --- | --- | --- |
| `admin` | `admin123` | 超级管理员 | 全部功能 + 管理员管理 |
| `auditor` | `auditor123` | 审核员 | 留言审核/删除、举报处理、会话管理 |
| `viewer` | `viewer123` | 只读访客 | 仅可查看留言与举报，不能修改 |

> 老版本升级：执行 `database/migration_add_admin_sessions.sql` 即可补齐
> 角色/状态字段与 `admin_sessions`、`login_logs` 两张新表。

## 项目结构

```
label-9900013/
├── index.php              # 首页
├── submit.php             # 发布留言页
├── detail.php             # 留言详情页
├── install.php            # 安装脚本
├── config/
│   └── database.php       # 数据库配置
├── includes/
│   ├── functions.php      # 公共函数
│   ├── auth.php           # 后台鉴权/会话/登录风控
│   ├── header.php         # 前台头部
│   └── footer.php         # 前台底部
├── api/
│   └── submit.php         # 留言提交API
├── admin/
│   ├── index.php          # 后台管理页
│   ├── login.php          # 后台登录
│   ├── api.php            # 后台API（按角色鉴权）
│   ├── logout.php         # 退出登录
│   ├── sessions.php       # 会话管理
│   ├── admins.php         # 管理员管理（仅超级管理员）
│   ├── reports.php        # 举报管理
│   ├── sidebar.php        # 后台公共侧边栏
│   ├── admin-auth.js      # 多标签页会话守卫
│   └── header.php         # 后台头部
├── assets/
│   ├── css/
│   │   └── style.css      # 样式文件
│   └── js/
│       └── main.js        # 脚本文件
└── uploads/               # 图片上传目录
```

## 数据库配置

编辑 `config/database.php` 文件：

```php
define('DB_HOST', 'localhost');
define('DB_USER', 'root');
define('DB_PASS', '');
define('DB_NAME', 'community_board');
```

## 使用说明

### 前台功能

1. **浏览留言**：首页展示所有已审核通过的留言
2. **筛选类型**：点击类型标签筛选特定类型的留言
3. **排序方式**：支持按时间或热度排序
4. **发布留言**：点击"发布留言"进入提交页面
5. **查看详情**：点击留言卡片查看完整内容

### 后台管理

1. 登录后台管理系统
2. 查看所有留言（支持状态、类型筛选和关键词搜索）
3. 审核留言（通过/拒绝）
4. 删除不当留言
5. 查看留言详情
6. 会话管理：查看当前账号所有活跃设备，可单独退出某设备或一键退出其他全部设备
7. 管理员管理（仅超级管理员）：新增账号、调整角色、停用/启用、重置密码

### 安全策略

- 连续登录失败 5 次锁定 15 分钟，登录页明确提示剩余尝试次数与锁定时间
- 会话空闲 30 分钟或登录满 2 小时自动过期，过期后明确提示并回到登录页
- 账号被停用、角色被修改时，所有已登录设备立即失效并提示原因
- 重新登录会重建会话，旧标签页不会沿用之前的权限状态

## 注意事项

- 安装完成后务必删除 `install.php`
- 修改默认管理员密码
- 确保 `uploads/` 目录有写入权限
- 建议配置 HTTPS 保障数据传输安全
